import type { LayoutLoad } from './$types';

export const load: LayoutLoad = () => {
	// TODO: Add authentication checker here, if user is already logged in, redirect him/her/they/them to dashboard :>
};
